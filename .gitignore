# Generated data — large / source-specific, never committed.
snapshots/
review/
promote/
*.log
.env

# Per-host compose overrides (real IPs, live-commit flag) — never shared.
docker-compose.override.yml

# Python
__pycache__/
*.pyc

# Eval harness venv (dev-only; recreated from eval/README.md)
eval/.venv/

# Per-host session resume notes — point at out-of-repo session-state.md
RESUME.md
